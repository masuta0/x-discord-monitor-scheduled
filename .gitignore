# Credentials and session data
auth.json
cookies*.json
cokies*.json
*.secret
.env
.env.*

# Local/generated runtime files
node_modules/
# seen.json is committed intentionally so scheduled runs remember processed invites.

# Logs and editor files
*.log
.DS_Store
